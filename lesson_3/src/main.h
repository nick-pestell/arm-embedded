#ifndef _VVC_MAIN_H
#define _VVC_MAIN_H

#include <stdint.h>
#include "stm32f722xx.h"

// Define GPIOE pin mappings for our button.
#define BUTTON_PIN_INPUT (0) // using PE0

// Define GPIOA pin mappings for our button.
#define BUTTON_PIN_POWER (0) // using PA0 to power button

// Define GPIOB pin mappings for out LED.
#define LED_PIN    (0) //user led is on PB0

#endif
